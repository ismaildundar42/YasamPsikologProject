@model YasamPsikologProject.WebUi.Models.DTOs.PsychologistDto
@{
    Layout = "_AdminLayout";
    ViewData["PageTitle"] = Model?.Id > 0 ? "Psikolog Düzenle" : "Yeni Psikolog";
    var isEdit = Model?.Id > 0;
}

<div class="row">
    <div class="col-md-8 offset-md-2">
        <div class="card card-primary">
            <div class="card-header">
                <h3 class="card-title">@ViewData["PageTitle"]</h3>
            </div>
            <form asp-action="@(isEdit ? "Edit" : "Create")" method="post">
                @if (isEdit)
                {
                    <input type="hidden" asp-for="Id" />
                    <input type="hidden" asp-for="UserId" />
                }
                
                <div class="card-body">
                    @if (TempData["ErrorMessage"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <i class="fas fa-exclamation-triangle"></i> @TempData["ErrorMessage"]
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                    }
                    
                    <div asp-validation-summary="All" class="text-danger"></div>
                    
                    <!-- Kullanıcı Bilgileri -->
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>Ad *</label>
                                <input type="text" class="form-control" name="User.FirstName" value="@Model?.User?.FirstName" 
                                       data-val="true" data-val-required="Ad alanı zorunludur" required>
                                <span class="text-danger field-validation-valid" data-valmsg-for="User.FirstName" data-valmsg-replace="true"></span>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>Soyad *</label>
                                <input type="text" class="form-control" name="User.LastName" value="@Model?.User?.LastName" 
                                       data-val="true" data-val-required="Soyad alanı zorunludur" required>
                                <span class="text-danger field-validation-valid" data-valmsg-for="User.LastName" data-valmsg-replace="true"></span>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>Email *</label>
                                <input type="email" class="form-control" name="User.Email" value="@Model?.User?.Email" 
                                       data-val="true" data-val-required="Email alanı zorunludur" 
                                       data-val-email="Geçerli bir email adresi giriniz" required>
                                <span class="text-danger field-validation-valid" data-valmsg-for="User.Email" data-valmsg-replace="true"></span>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>Telefon *</label>
                                <input type="tel" class="form-control" name="User.PhoneNumber" value="@Model?.User?.PhoneNumber" 
                                       pattern="0[0-9]{10}" placeholder="05551234567"
                                       data-val="true" data-val-required="Telefon alanı zorunludur" required>
                                <small class="form-text text-muted">Format: 05551234567 (11 hane)</small>
                                <span class="text-danger field-validation-valid" data-valmsg-for="User.PhoneNumber" data-valmsg-replace="true"></span>
                            </div>
                        </div>                        <div class="col-md-6">
                            <div class="form-group">
                                <label>Yeni Şifre</label>
                                <input type="password" class="form-control" name="User.Password" 
                                       data-val-minlength="Şifre en az 6 karakter olmalıdır" 
                                       data-val-minlength-min="6" minlength="6">
                                <small class="form-text text-muted">Değiştirmek istemiyorsanız boş bırakın (En az 6 karakter)</small>
                                <span class="text-danger field-validation-valid" data-valmsg-for="User.Password" data-valmsg-replace="true"></span>
                            </div>
                        </div>                    </div>

                    <!-- Psikolog Bilgileri -->
                    <hr>
                    <h5 class="mb-3">Görsel Ayarlar</h5>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>Takvim Rengi *</label>
                                <input type="color" class="form-control" asp-for="CalendarColor" 
                                       value="@(Model?.CalendarColor ?? "#4CAF50")" required>
                                <small class="form-text text-muted">Randevu rengi</small>
                            </div>
                        </div>
                    </div>

                    <hr>
                    <h5 class="mb-3">Randevu Tipleri</h5>
                    
                    <div class="form-group">
                        <p class="text-muted mb-3">
                            <i class="fas fa-info-circle"></i> Psikologun kabul edeceği randevu tiplerini seçiniz
                        </p>
                        
                        <div class="custom-control custom-radio mb-2">
                            <input type="radio" class="custom-control-input" name="consultationType" 
                                   id="consultationBoth" value="both" 
                                   checked="@(Model.IsOnlineConsultationAvailable && Model.IsInPersonConsultationAvailable)"
                                   onclick="updateConsultationType('both')">
                            <label class="custom-control-label" for="consultationBoth">
                                <i class="fas fa-check-double text-success"></i> <strong>Her İkisi</strong> (Online ve Yüz Yüze)
                            </label>
                        </div>
                        
                        <div class="custom-control custom-radio mb-2">
                            <input type="radio" class="custom-control-input" name="consultationType" 
                                   id="consultationOnline" value="online"
                                   checked="@(Model.IsOnlineConsultationAvailable && !Model.IsInPersonConsultationAvailable)"
                                   onclick="updateConsultationType('online')">
                            <label class="custom-control-label" for="consultationOnline">
                                <i class="fas fa-video text-primary"></i> Sadece Online Görüşme
                            </label>
                        </div>
                        
                        <div class="custom-control custom-radio mb-2">
                            <input type="radio" class="custom-control-input" name="consultationType" 
                                   id="consultationInPerson" value="inperson"
                                   checked="@(!Model.IsOnlineConsultationAvailable && Model.IsInPersonConsultationAvailable)"
                                   onclick="updateConsultationType('inperson')">
                            <label class="custom-control-label" for="consultationInPerson">
                                <i class="fas fa-user text-info"></i> Sadece Yüz Yüze Görüşme
                            </label>
                        </div>
                        
                        <!-- Hidden inputs for actual form submission -->
                        <input type="hidden" asp-for="IsOnlineConsultationAvailable" id="IsOnlineConsultationAvailable" value="@Model.IsOnlineConsultationAvailable.ToString().ToLower()" />
                        <input type="hidden" asp-for="IsInPersonConsultationAvailable" id="IsInPersonConsultationAvailable" value="@Model.IsInPersonConsultationAvailable.ToString().ToLower()" />
                    </div>

                    <hr>
                    <h5 class="mb-3">Diğer Ayarlar</h5>

                    <div class="form-group">
                        <div class="custom-control custom-switch">
                            <input type="checkbox" class="custom-control-input" asp-for="IsActive" checked>
                            <label class="custom-control-label" for="IsActive">Aktif</label>
                        </div>
                    </div>
                </div>

                <div class="card-footer">
                    <button type="submit" class="btn btn-success">
                        <i class="fas fa-save"></i> Kaydet
                    </button>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="fas fa-times"></i> İptal
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        // Randevu tipi güncellemesi
        function updateConsultationType(type) {
            var onlineInput = document.getElementById('IsOnlineConsultationAvailable');
            var inPersonInput = document.getElementById('IsInPersonConsultationAvailable');
            
            switch(type) {
                case 'both':
                    onlineInput.value = 'true';
                    inPersonInput.value = 'true';
                    break;
                case 'online':
                    onlineInput.value = 'true';
                    inPersonInput.value = 'false';
                    break;
                case 'inperson':
                    onlineInput.value = 'false';
                    inPersonInput.value = 'true';
                    break;
            }
        }
        
        $(document).ready(function() {
            // Form submit handler
            $('form').on('submit', function(e) {
                var form = $(this);
                
                // Client-side validation check
                if (!form.valid()) {
                    e.preventDefault();
                    toastr.error('Lütfen tüm alanları doğru şekilde doldurunuz.');
                    return false;
                }
                
                // Disable submit button
                var submitBtn = form.find('button[type="submit"]');
                submitBtn.prop('disabled', true).html('<i class="fas fa-spinner fa-spin"></i> Kaydediliyor...');
            });

            // Telefon input için mask
            $('input[name="User.PhoneNumber"]').on('input', function() {
                var value = $(this).val().replace(/\D/g, '');
                if (value.length > 11) {
                    value = value.substring(0, 11);
                }
                $(this).val(value);
            });
        });
    </script>
}
